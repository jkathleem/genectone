import Link from "next/link";
import { notFound } from "next/navigation";
import { PageHeader } from "@/components/page-header";
import { formatCurrency, formatDate } from "@/lib/format";
import { prisma } from "@/lib/prisma";
import { receivableRemainingAmount, receivableStatus, receivedAmount } from "@/modules/accounts-receivable/domain";
export default async function Page({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const account = await prisma.accountReceivable.findUnique({ where: { id }, include: { company: true, customer: true, allocations: { include: { receipt: true }, orderBy: { receipt: { receiptDate: "desc" } } }, billing: { include: { productionOrder: true } } } }); if (!account) notFound(); const received = receivedAmount(account.allocations), remaining = receivableRemainingAmount(account.originalAmount, account.allocations); return <><PageHeader title="Conta a Receber" description="Faturamento, obrigação e recebimento são fatos distintos." action={{ label: "Voltar às contas", href: "/financeiro/contas-a-receber" }}/><section className="panel mb-5"><dl className="detail-grid"><div><dt>Empresa</dt><dd>{account.company.tradeName || account.company.name}</dd></div><div><dt>Cliente</dt><dd>{account.customer.name}</dd></div><div><dt>Descrição</dt><dd>{account.description}</dd></div><div><dt>OP</dt><dd><Link className="link-button" href={`/ops/${account.billing.productionOrderId}`}>{account.billing.productionOrder.number}</Link></dd></div><div><dt>Nº da NFe</dt><dd>{account.billing.invoiceNumber}</dd></div><div><dt>Emissão</dt><dd>{formatDate(account.billing.issueDate)}</dd></div><div><dt>Competência</dt><dd>{formatDate(account.competenceDate)}</dd></div><div><dt>Vencimento</dt><dd>{formatDate(account.dueDate)}</dd></div><div><dt>Valor original</dt><dd>{formatCurrency(account.originalAmount)}</dd></div><div><dt>Recebido</dt><dd>{formatCurrency(received)}</dd></div><div><dt>Saldo</dt><dd>{formatCurrency(remaining)}</dd></div><div><dt>Situação</dt><dd>{receivableStatus(account.originalAmount, account.dueDate, account.allocations)}</dd></div><div><dt>Criado em</dt><dd>{account.createdAt.toLocaleString("pt-BR")}</dd></div></dl></section><section className="panel"><h2 className="section-title mb-4">Recebimentos</h2>{account.allocations.length ? <div className="table-wrap"><table><thead><tr><th>Data</th><th>Alocado nesta conta</th><th>Total do recebimento</th><th>Observações</th><th/></tr></thead><tbody>{account.allocations.map(item => <tr key={item.id}><td>{formatDate(item.receipt.receiptDate)}</td><td>{formatCurrency(item.amount)}</td><td>{formatCurrency(item.receipt.amount)}</td><td>{item.receipt.notes || "—"}</td><td><Link className="link-button" href={`/financeiro/recebimentos/${item.receiptId}`}>Abrir</Link></td></tr>)}</tbody></table></div> : <p className="empty-state">Nenhum recebimento registrado.</p>}</section></>; }
