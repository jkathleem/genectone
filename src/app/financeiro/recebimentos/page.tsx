import Link from "next/link";
import { PageHeader } from "@/components/page-header";
import { formatCurrency, formatDate } from "@/lib/format";
import { prisma } from "@/lib/prisma";
type Params = { companyId?: string; customerId?: string; from?: string; to?: string };
export default async function Page({ searchParams }: { searchParams: Promise<Params> }) { const p = await searchParams, receiptDate = p.from || p.to ? { gte: p.from ? new Date(`${p.from}T00:00:00Z`) : undefined, lte: p.to ? new Date(`${p.to}T00:00:00Z`) : undefined } : undefined; const [rows, companies, customers] = await Promise.all([prisma.receipt.findMany({ where: { companyId: p.companyId || undefined, customerId: p.customerId || undefined, receiptDate }, include: { company: true, customer: true, allocations: { select: { id: true } } }, orderBy: [{ receiptDate: "desc" }, { createdAt: "desc" }] }), prisma.company.findMany({ orderBy: { name: "asc" } }), prisma.customer.findMany({ orderBy: { name: "asc" } })]); return <><PageHeader title="Recebimentos" description="Entradas financeiras reais, integralmente alocadas às Contas a Receber." action={{ label: "Novo recebimento", href: "/financeiro/recebimentos/novo" }}/><form className="panel mb-5 form-grid"><label className="field">Empresa<select defaultValue={p.companyId || ""} name="companyId"><option value="">Todas</option>{companies.map(x => <option key={x.id} value={x.id}>{x.tradeName || x.name}</option>)}</select></label><label className="field">Cliente<select defaultValue={p.customerId || ""} name="customerId"><option value="">Todos</option>{customers.map(x => <option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label className="field">Recebimento de<input defaultValue={p.from} name="from" type="date"/></label><label className="field">Recebimento até<input defaultValue={p.to} name="to" type="date"/></label><div className="flex items-end gap-2"><button className="button-primary">Filtrar</button><Link className="button-secondary" href="/financeiro/recebimentos">Limpar</Link></div></form><section className="panel">{rows.length ? <div className="table-wrap"><table><thead><tr><th>Data</th><th>Empresa</th><th>Cliente</th><th>Valor</th><th>Contas</th><th>Observações</th><th>Criado em</th><th/></tr></thead><tbody>{rows.map(row => <tr key={row.id}><td>{formatDate(row.receiptDate)}</td><td>{row.company.tradeName || row.company.name}</td><td>{row.customer.name}</td><td>{formatCurrency(row.amount)}</td><td>{row.allocations.length}</td><td>{row.notes || "—"}</td><td>{row.createdAt.toLocaleString("pt-BR")}</td><td><Link className="link-button" href={`/financeiro/recebimentos/${row.id}`}>Abrir</Link></td></tr>)}</tbody></table></div> : <p className="empty-state">Nenhum recebimento registrado.</p>}</section></>; }
