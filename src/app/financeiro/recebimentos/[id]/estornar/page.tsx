import{notFound}from"next/navigation";import{PageHeader}from"@/components/page-header";import{SubmitButton}from"@/components/submit-button";import{formatCurrency,formatDate}from"@/lib/format";import{prisma}from"@/lib/prisma";import{reverseReceiptAction}from"@/modules/financial-reversals/actions";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,r=await prisma.receipt.findUnique({where:{id},include:{reversal:true,company:true,customer:true}});if(!r)notFound();return <><PageHeader title="Estornar recebimento" description="Todas as alocações serão desfeitas economicamente, sem exclusão." action={{label:"Cancelar",href:`/financeiro/recebimentos/${id}`}}/><section className="panel mb-4"><p>{formatDate(r.receiptDate)} · {formatCurrency(r.amount)} · {r.customer.name}</p></section><form action={reverseReceiptAction} className="panel form-grid"><input type="hidden" name="receiptId" value={id}/><label className="field">Data do estorno<input type="date" name="reversalDate" min={r.receiptDate.toISOString().slice(0,10)} required/></label><label className="field sm:col-span-2">Motivo<textarea name="reason" required/></label><SubmitButton disabled={!!r.reversal}>Confirmar estorno total</SubmitButton></form></>}
