import Link from"next/link";import{notFound}from"next/navigation";import{PageHeader}from"@/components/page-header";import{formatCurrency,formatDate}from"@/lib/format";import{prisma}from"@/lib/prisma";import{financialStatus,paidAmount,remainingAmount}from"@/modules/accounts-payable/domain";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const a=await prisma.accountPayable.findUnique({where:{id},include:{company:true,contractorSettlement:{include:{contractor:true}}}});if(!a)notFound();return <><PageHeader title="Conta a Pagar" description="Obrigação financeira; não representa pagamento realizado."/><section className="panel mb-5"><dl className="detail-grid"><div><dt>Empresa</dt><dd>{a.company.tradeName||a.company.name}</dd></div><div><dt>Descrição</dt><dd>{a.description}</dd></div><div><dt>Origem</dt><dd><Link className="link-button" href={`/terceirizacao/fechamentos/${a.contractorSettlementId}`}>Fechamento de Terceirizados</Link></dd></div><div><dt>Terceirizado</dt><dd>{a.contractorSettlement.contractor.name}</dd></div><div><dt>Competência</dt><dd>{formatDate(a.competenceDate)}</dd></div><div><dt>Vencimento</dt><dd>{formatDate(a.dueDate)}</dd></div><div><dt>Valor original</dt><dd>{formatCurrency(a.originalAmount)}</dd></div><div><dt>Pago</dt><dd>{formatCurrency(paidAmount())}</dd></div><div><dt>Saldo</dt><dd>{formatCurrency(remainingAmount(a.originalAmount))}</dd></div><div><dt>Situação</dt><dd>{financialStatus(a.dueDate)}</dd></div><div><dt>Criado em</dt><dd>{a.createdAt.toLocaleString("pt-BR")}</dd></div></dl></section><section className="panel"><h2 className="section-title">Pagamentos</h2><p className="empty-state mt-4">Nenhum pagamento registrado. O registro de pagamentos será implementado na próxima etapa.</p></section></>}
