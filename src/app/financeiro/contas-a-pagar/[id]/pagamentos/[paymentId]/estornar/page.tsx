import{notFound}from"next/navigation";import{PageHeader}from"@/components/page-header";import{SubmitButton}from"@/components/submit-button";import{formatCurrency,formatDate}from"@/lib/format";import{prisma}from"@/lib/prisma";import{reversePaymentAction}from"@/modules/financial-reversals/actions";
export default async function Page({params}:{params:Promise<{id:string;paymentId:string}>}){const{id,paymentId}=await params,p=await prisma.payment.findUnique({where:{id:paymentId},include:{reversal:true,accountPayable:true}});if(!p||p.accountPayableId!==id)notFound();return <><PageHeader title="Estornar pagamento" description="O pagamento original será preservado." action={{label:"Cancelar",href:`/financeiro/contas-a-pagar/${id}`}}/><section className="panel mb-4"><p>{formatDate(p.paymentDate)} · {formatCurrency(p.amount)} · {p.accountPayable.payeeName}</p></section><form action={reversePaymentAction} className="panel form-grid"><input type="hidden" name="paymentId" value={p.id}/><input type="hidden" name="accountId" value={id}/><label className="field">Data do estorno<input type="date" name="reversalDate" min={p.paymentDate.toISOString().slice(0,10)} required/></label><label className="field sm:col-span-2">Motivo<textarea name="reason" required/></label><SubmitButton disabled={!!p.reversal}>Confirmar estorno total</SubmitButton></form></>}
