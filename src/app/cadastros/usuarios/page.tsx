import{prisma}from"@/lib/prisma";import{requireUser}from"@/modules/auth/session";import{createUserAction,toggleUserAction,updateUserAction}from"@/modules/users/actions";
const roles=["ADMIN","FINANCE","OPERATIONS","VIEWER"]as const,field="rounded-md border border-slate-300 px-3 py-2 text-sm";
export default async function UsersPage(){await requireUser("ADMIN");const users=await prisma.user.findMany({orderBy:[{active:"desc"},{name:"asc"}]});return <div><h1 className="text-2xl font-semibold">Usuários</h1><p className="mt-1 text-sm text-slate-500">Acesso, perfil e situação dos usuários.</p><form action={createUserAction} className="mt-6 grid gap-3 rounded-lg border bg-white p-4 md:grid-cols-4"><input className={field} name="name" placeholder="Nome" required/><input className={field} name="email" placeholder="E-mail" required type="email"/><input className={field} minLength={8} name="password" placeholder="Senha inicial" required type="password"/><select className={field} name="role">{roles.map(x=><option key={x}>{x}</option>)}</select><button className="rounded-md bg-[var(--brand)] px-4 py-2 text-sm font-semibold text-white md:col-span-4">Criar usuário</button></form><div className="mt-6 space-y-3">{users.map(u=><form action={updateUserAction} className="grid gap-3 rounded-lg border bg-white p-4 md:grid-cols-5" key={u.id}><input name="id" type="hidden" value={u.id}/><input className={field} defaultValue={u.name} name="name" required/><input className={`${field} bg-slate-50`} disabled value={u.email}/><select className={field} defaultValue={u.role} name="role">{roles.map(x=><option key={x}>{x}</option>)}</select><input className={field} minLength={8} name="password" placeholder="Nova senha (opcional)" type="password"/><button className="rounded-md border px-3 py-2 text-sm font-semibold">Salvar</button><div className="md:col-span-5"><button className="text-sm font-semibold text-[var(--brand)]" formAction={toggleUserAction} name="active" value={String(!u.active)}>{u.active?"Desativar":"Ativar"}</button></div></form>)}</div></div>}
