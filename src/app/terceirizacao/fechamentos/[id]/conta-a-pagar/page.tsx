import{notFound,redirect}from"next/navigation";import{PageHeader}from"@/components/page-header";import{formatCurrency}from"@/lib/format";import{prisma}from"@/lib/prisma";import{settlementTotal}from"@/modules/contractor-settlements/domain";import{generateAccountPayable}from"@/modules/accounts-payable/actions";
export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await prisma.contractorSettlement.findUnique({where:{id},include:{company:true,contractor:true,items:true,accountPayable:true}});if(!s)notFound();if(s.status!=="APPROVED")redirect(`/terceirizacao/fechamentos/${id}`);if(s.accountPayable)redirect(`/financeiro/contas-a-pagar/${s.accountPayable.id}`);const total=settlementTotal(s.items.map(i=>({quantity:i.approvedQuantityIncluded,price:i.appliedUnitPriceSnapshot})));return <><PageHeader title="Gerar Conta a Pagar" description="A obrigação será criada a partir do fechamento aprovado."/><section className="panel mb-5"><dl className="detail-grid"><div><dt>Empresa</dt><dd>{s.company.tradeName||s.company.name}</dd></div><div><dt>Terceirizado</dt><dd>{s.contractor.name}</dd></div><div><dt>Competência</dt><dd>{String(s.periodMonth).padStart(2,"0")}/{s.periodYear}</dd></div><div><dt>Valor</dt><dd>{formatCurrency(total)}</dd></div></dl></section><form action={generateAccountPayable} className="panel form-grid"><input name="settlementId" type="hidden" value={id}/><label className="field">Vencimento<input name="dueDate" required type="date"/></label><div className="flex items-end"><button className="button-primary">Gerar Conta a Pagar</button></div></form></>}
