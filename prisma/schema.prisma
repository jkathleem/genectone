generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Domain models will be added in a later phase after reviewing docs/DOMAIN_MODEL.md.
